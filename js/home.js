/* home.js – Home page initialisation */

document.addEventListener("DOMContentLoaded", function () {
  // Ensure localStorage is seeded with predefined quizzes on first visit
  initializeData();
});
